// Re-export from core — the canonical definitions now live in @true-recall/core
export {
	DeviceDiscoveryService,
	type DeviceDatabaseInfo,
} from "@true-recall/core/integration/device-discovery.service";
